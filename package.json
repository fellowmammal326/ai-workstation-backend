{
  "name": "ai-workstation-monorepo-root",
  "version": "1.0.0",
  "description": "Root package for AI Workstation monorepo to orchestrate build and start scripts for Render.",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix backend && npm run build --prefix backend",
    "start": "npm start --prefix backend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}